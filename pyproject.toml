[project]
name = "parking-rag-bot"
version = "1.0.0"
description = "Stage 1 — Parking RAG chatbot (LangChain + LangGraph + Milvus Lite)"
requires-python = ">=3.11,<3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools]
packages = ["parking_bot"]
