# --- Core LLM / orchestration ---
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-community>=0.4.0
langgraph>=1.0.0

# --- Vector database (Milvus Lite: embedded, local) ---
# Used directly via pymilvus' MilvusClient (embedded mode).
pymilvus>=2.5,<2.6
milvus-lite>=2.4.0

# --- Optional: local embeddings backend (no API key needed) ---
# Enable with EMBEDDING_PROVIDER=huggingface
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# --- Guardrails: pre-trained NLP (PII detection) ---
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0

# --- UI ---
streamlit>=1.40.0
# Auto-refresh the chat so admin decisions surface without user input (optional).
streamlit-autorefresh>=1.0.1

# --- Admin agent REST server (Stage 2) ---
fastapi>=0.110.0
uvicorn>=0.29.0
# Required by FastAPI to parse the admin page's HTML form submissions.
python-multipart>=0.0.9

# --- MCP reservation-store server + client (Stage 3) ---
mcp>=1.2.0

# --- Utils ---
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Testing / eval ---
pytest>=8.0.0
pytest-mock>=3.12.0
